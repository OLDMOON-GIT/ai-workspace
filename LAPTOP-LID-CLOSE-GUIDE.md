# 노트북 화면 닫기 시 계속 동작하도록 설정 가이드

## 🎯 목표
노트북 화면을 닫아도 노트북이 계속 동작하도록 설정합니다.

---

## 📌 방법 1: 제어판에서 수동 설정 (권장)

### 단계별 가이드

#### 1️⃣ 전원 옵션 열기
```
Windows 검색창 → "전원 옵션" 입력 → "전원 옵션" 클릭
또는
제어판 → 전원 옵션
```

#### 2️⃣ 현재 전원 계획의 설정 변경
```
좌측 메뉴 → "현재 활성화된 계획의 설정 변경" 클릭
```

#### 3️⃣ 고급 전원 설정 변경
```
하단의 "고급 전원 설정 변경" 링크 클릭
```

#### 4️⃣ 전원 버튼 및 덮개 설정
```
트리 구조에서 "전원 버튼 및 덮개"를 찾아 확장
```

#### 5️⃣ 덮개를 닫을 때 설정 변경
```
"덮개를 닫을 때:" 항목을 찾음
├─ AC 전원: "아무 것도 하지 않음" 선택
└─ 배터리: "아무 것도 하지 않음" 선택
```

#### 6️⃣ 적용 및 확인
```
"적용" 버튼 클릭
"확인" 버튼 클릭
```

### 결과 ✅
화면을 닫아도 노트북이 계속 동작합니다.

---

## 📌 방법 2: 배치 파일로 자동 설정

### 파일 위치
```
C:\Users\oldmoon\workspace\set-lid-close-do-nothing.bat
```

### 실행 방법
```
1. 시작 메뉴 → PowerShell (관리자)로 실행
2. 다음 명령 실행:
   powercfg /query SCHEME_CURRENT

3. 출력된 GUID를 확인 (예: {381b4222-f694-41f0-9685-ff5bb260df2e})
4. set-lid-close-do-nothing.bat 파일 우클릭 → "관리자 권한으로 실행"
```

### 주의사항 ⚠️
- **관리자 권한 필수**
- GUID는 시스템마다 다를 수 있음
- 실패 시 방법 1(수동 설정)을 사용

---

## 📌 방법 3: PowerShell로 명령 실행

### 1. PowerShell을 관리자 권한으로 실행

```powershell
# 현재 활성 전원 계획의 GUID 확인
powercfg /list
# 또는
powercfg /getactivescheme
```

### 2. 해당 GUID로 설정 변경

```powershell
# 변수에 GUID 저장 (예시)
$PLAN = "{381b4222-f694-41f0-9685-ff5bb260df2e}"
$SUBGROUP = "4f971e89-eebd-4455-a8de-9e59040e7347"
$LIDAC = "5ca83367-6e7e-460b-b5c5-c54fb6427ed1"
$LIDDC = "7e8ba16a-e360-435b-aca5-33a50e5dbf63"

# AC 전원에서 아무 것도 하지 않음 (0)
powercfg /setacvalueindex $PLAN $SUBGROUP $LIDAC 0

# 배터리 모드에서 아무 것도 하지 않음 (0)
powercfg /setdcvalueindex $PLAN $SUBGROUP $LIDDC 0

# 설정 적용
powercfg /S $PLAN
```

### 설정 값 의미

| 값 | 의미 |
|----|------|
| 0 | 아무 것도 하지 않음 |
| 1 | 대기 모드 |
| 2 | 절전 모드 |
| 3 | 컴퓨터 종료 |

---

## 🔍 설정 확인 및 되돌리기

### 현재 설정 확인

```powershell
powercfg /query SCHEME_CURRENT
```

### 기본값으로 되돌리기

```powershell
# 기본 설정으로 복원
powercfg /restoredefaultschemes

# 또는 개별 설정 변경
powercfg /setacvalueindex SCHEME_CURRENT 4f971e89-eebd-4455-a8de-9e59040e7347 5ca83367-6e7e-460b-b5c5-c54fb6427ed1 1
```

---

## 💡 추가 팁

### 시작 프로그램 설정
노트북 화면을 닫을 때 특정 프로그램이 계속 실행되게 하려면:

```
Windows 검색 → "시작" → "시작 폴더 열기"
→ 프로그램 바로가기를 폴더에 추가
```

### 배터리 절약 모드
화면을 닫고 작업이 진행 중일 때:
- 디스플레이: 절전
- 프로세서: 계속 100% 동작
- 배터리: 소비됨

### 모니터 외부 연결
더 오래 사용할 계획이면:
```
USB-C/HDMI 외부 모니터 연결
→ 노트북 닫기 → 외부 모니터에서 작업 계속
```

---

## 🎯 확인 방법

### 1. 재부팅 후 확인
```
1. 위의 설정 완료
2. 노트북 재부팅
3. 화면 닫기
4. 작은 LED 또는 팬 소리로 동작 확인
```

### 2. 작업 관리자 확인
```
Ctrl + Shift + Esc
→ 프로세스 탭에서 실행 중인 프로그램 확인
```

### 3. 원격 접속으로 확인
```
다른 컴퓨터에서 SSH 또는 원격 데스크톱으로 접속
→ 노트북이 계속 실행 중인지 확인
```

---

## ⚠️ 주의사항

| 항목 | 내용 |
|------|------|
| 배터리 | 화면 닫음 → 배터리 빠르게 소비 |
| 발열 | 장시간 사용 시 노트북 과열 가능 |
| 보안 | 화면 닫아도 계속 실행 → 보안 고려 |
| 절전 | 절전 모드가 아님 → 전력 소비 계속 |

**권장**:
- 장시간 미사용: 절전 또는 최대 절전 모드 사용
- 작업 진행 중: 현재 설정(아무 것도 하지 않음) 사용

---

## 📞 문제 해결

### 문제: "지정한 전원 구성표가 없습니다"
```
→ GUID가 잘못되었을 가능성
→ powercfg /list로 정확한 GUID 확인 후 재시도
```

### 문제: 권한 부족
```
→ 관리자 권한으로 PowerShell/CMD 실행 필요
→ Windows 검색 → PowerShell → 우클릭 → 관리자 권한으로 실행
```

### 문제: 설정이 적용되지 않음
```
→ 노트북 재부팅 필요
→ 재부팅 후 다시 확인
```

---

## 📝 요약

| 작업 | 명령 |
|------|------|
| 전원 설정 열기 | `control powercfg` |
| 현재 계획 확인 | `powercfg /getactivescheme` |
| 모든 GUID 확인 | `powercfg /list` |
| 설정 변경 (AC) | `powercfg /setacvalueindex [PLAN] [SUBGROUP] [SETTING] 0` |
| 설정 변경 (배터리) | `powercfg /setdcvalueindex [PLAN] [SUBGROUP] [SETTING] 0` |
| 기본값 복구 | `powercfg /restoredefaultschemes` |

---

**✨ 완료!**

이제 노트북 화면을 닫아도 계속 동작합니다! 🎉
