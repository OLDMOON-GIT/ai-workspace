# 자동화 페이지 통합 테스트 가이드

## 📋 개요

이 문서는 자동화 페이지의 통합 테스트 실행 방법을 설명합니다.

## 🎯 테스트 파일

### 1. `test-automation-integration-complete.js`
**상세한 통합 테스트 (15개 항목)**

테스트 항목:
- ✅ 제목 추가
- ✅ 제목 조회
- ✅ 제목 수정
- ✅ 제목 삭제
- ✅ 스케줄 추가
- ✅ 스케줄 조회
- ✅ 스케줄 수정
- ✅ 스케줄 상태 업데이트
- ✅ 스케줄 삭제
- ✅ 로그 조회
- ✅ 유효성 검증 (필수 필드)
- ✅ 유효성 검증 (타입)
- ✅ 카테고리 조회
- ✅ 채널 설정 조회

실행:
```bash
node test-automation-integration-complete.js
```

### 2. `test-automation-integration-v2.js`
**디버깅 정보가 포함된 버전**

특징:
- 각 API의 상태 코드 출력
- 응답 본문 출력
- 에러 메시지 표시
- 진단 정보 제공

실행:
```bash
node test-automation-integration-v2.js
```

### 3. `test-automation-complete-flow.js`
**완전한 플로우 테스트 (20개 항목)**

테스트 범위:
- 📚 제목 CRUD 작업
- 📅 스케줄 CRUD 작업
- ✔️ 유효성 검증
- ⚙️ 기타 API
- 🗑️ 정리 및 확인

실행:
```bash
node test-automation-complete-flow.js
```

## 🚀 실행 전 체크리스트

### 1. 서버 실행
```bash
cd trend-video-frontend
npm run dev
```

서버가 `http://localhost:3000`에서 실행 중인지 확인하세요.

### 2. 데이터베이스 준비
```bash
# 데이터베이스 파일 존재 확인
ls -la data/database.sqlite
```

### 3. 환경 변수 확인
`.env.local` 또는 `.env`에 다음 설정이 있는지 확인:
```
# 데이터베이스 경로
DATABASE_URL=./data/database.sqlite

# 기타 필요한 설정
```

## 🧪 테스트 실행

### 모든 테스트 실행
```bash
# 기본 테스트
node test-automation-integration-complete.js

# 디버깅 버전
node test-automation-integration-v2.js

# 완전한 플로우
node test-automation-complete-flow.js
```

### 특정 테스트만 실행
파일을 수정하여 필요한 테스트만 주석 처리하거나 선택적으로 실행할 수 있습니다.

## 📊 결과 해석

### 성공 (✅)
```
✅ 통과: 20개 테스트
📊 성공률: 100.0%
```

### 실패 (❌)
```
❌ 실패: 제목 추가
   원인: API 실패: 500
```

## 🔍 일반적인 문제 해결

### 모든 테스트가 500 에러

**원인:**
- 서버가 실행 중이지 않음
- 데이터베이스 파일이 없음
- 데이터베이스 경로 오류

**해결:**
```bash
# 1. 서버 실행 확인
npm run dev

# 2. 데이터베이스 파일 확인
ls -la data/database.sqlite

# 3. 서버 로그 확인
# npm run dev 출력 확인
```

### 401 Unauthorized 에러

**원인:**
- 사용자 인증이 필요함

**해결:**
- 테스트 스크립트에 인증 헤더 추가 필요
- 또는 `/api/automation` 라우트의 인증 설정 확인

### 데이터 중복 저장

**원인:**
- 이전 테스트가 정상 종료되지 않음

**해결:**
```bash
# 데이터베이스 초기화
rm data/database.sqlite
# 또는 관리자 페이지에서 수동 삭제
```

## 📝 테스트 시나리오

### 시나리오 1: 기본 CRUD
1. 제목 추가 ✓
2. 제목 조회 ✓
3. 제목 수정 ✓
4. 제목 삭제 ✓

### 시나리오 2: 스케줄 관리
1. 제목 생성
2. 스케줄 추가
3. 스케줄 상태 변경 (scheduled → processing)
4. 스케줄 삭제
5. 제목 삭제

### 시나리오 3: 유효성 검증
- 필수 필드 확인
- 타입 검증
- 범위 검증

## 🛠️ 테스트 커스터마이징

### 테스트 데이터 수정
각 파일의 `testData` 객체를 수정하세요:

```javascript
const testData = {
  title: `테스트_${Date.now()}`,  // 테스트 제목
  type: 'shortform',              // 타입
  category: '테스트',             // 카테고리
  model: 'claude',                // AI 모델
  mediaMode: 'upload'             // 미디어 생성 방식
};
```

### 새로운 테스트 추가
```javascript
await test('테스트 이름', async () => {
  const response = await makeRequest('GET', '/api/endpoint');
  // 검증 로직
  if (response.status !== 200) {
    throw new Error('테스트 실패 이유');
  }
});
```

## 📚 API 참고

### 제목 관리
```
POST   /api/automation/titles          - 제목 추가
GET    /api/automation/titles          - 제목 목록
PATCH  /api/automation/titles          - 제목 수정
DELETE /api/automation/titles?id={id}  - 제목 삭제
```

### 스케줄 관리
```
POST   /api/automation/schedules          - 스케줄 추가
GET    /api/automation/schedules          - 스케줄 목록
PATCH  /api/automation/schedules          - 스케줄 수정
DELETE /api/automation/schedules?id={id}  - 스케줄 삭제
```

### 설정 및 조회
```
GET /api/automation/categories       - 카테고리 목록
GET /api/automation/channel-settings - 채널 설정
GET /api/automation/logs             - 로그 조회
```

## 🎓 추가 학습

### API 응답 형식
제목 생성:
```json
{
  "success": true,
  "titleId": "uuid-string"
}
```

스케줄 생성:
```json
{
  "success": true,
  "scheduleId": "uuid-string"
}
```

### 필드명 매핑
JavaScript (camelCase) → Database (snake_case)
- `mediaMode` → `media_mode`
- `scriptMode` → `script_mode`
- `youtubePrivacy` → `youtube_privacy`
- `channelId` → `channel_id`

## 💡 팁

1. **병렬 테스트 실행 금지**
   - 테스트는 순차적으로 실행되어야 함
   - 한 번에 하나의 테스트 파일만 실행

2. **데이터 정리**
   - 각 테스트 후 생성한 데이터 자동 삭제
   - 수동 정리가 필요하면 관리자 페이지 사용

3. **로그 레벨 설정**
   - 상세한 디버깅: `test-automation-integration-v2.js` 사용
   - 빠른 확인: `test-automation-complete-flow.js` 사용

## ✅ 체크리스트

테스트 실행 전:
- [ ] 서버 실행 중 (`npm run dev`)
- [ ] 데이터베이스 파일 존재 (`data/database.sqlite`)
- [ ] 포트 3000 사용 가능
- [ ] 충분한 디스크 공간

테스트 후:
- [ ] 모든 테스트 결과 확인
- [ ] 실패 항목 분석
- [ ] 필요시 이슈 생성

---

**마지막 업데이트:** 2024년
**작성자:** Claude Code
