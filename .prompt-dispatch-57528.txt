BTS-14671 BUG 해결 요청

제목: 빌드 에러 디텍팅 안됨 - Next.js 빌드 출력이 로그 파일에 저장되지 않음
우선순위: P1
설명: 로그 모니터는 파일 기반 모니터링만 수행하므로, Next.js dev 서버의 빌드 에러(콘솔 출력)가 감지되지 않음. 해결책: 1) dev 서버 stdout/stderr를 로그 파일로 리다이렉트 또는 2) 주기적 빌드 체크 스크립트 추가

작업 완료 후 반드시 실행:
node bug.js resolve 14671 "해결 내용"
��으로 로드되는지 확인

await page.goto(BASE_URL + '/admin/tasks');
await page.waitForLoadState('networkidle');
// TODO: checkNoErrors - 

```

## 페이지 DOM 구조 힌트
```
URL: http://localhost:2000/auth

주요 셀렉터:
input[id="rememberMe", type="checkbox"]
button[text="로그인"]
```

## 수정 요청 사항
AI가 위 DOM 구조를 참고하여 실제 존재하는 selector로 test_code를 수정해야 합니다.
수정된 코드는 test_scenario 테이블의 해당 레코드에 UPDATE 해주세요.

작업 완료 후 반드시 실행:
node bug.js resolve 5445 "해결 내용"
